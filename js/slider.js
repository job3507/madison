(function($){
    jQuery.fn.pluginNae = function(options){
        var options = $.extend({
            leftBtn: 'leftBtn',
            rightBtn: 'rightBtn',
            quantly: 4,
            autoPlay: true,
            autoPlayDelay: 10,

        })
    }
})
